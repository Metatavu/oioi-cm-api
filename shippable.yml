language: java
jdk: openjdk11
branches:
  only:
    - master

env:
  global:

build:
  ci:
    - mvn clean package
    - docker build -t metatavu/oioi-cm-api:$BRANCH.BUILD_NUMBER .

integrations:
  hub:
    - integrationName: awskeys
      type: ecr
